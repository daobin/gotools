mysql:
  tags: dev
  dev:
    url: [username]:[password]@tcp([ip]:[port])/[dbName]?charset=utf8mb4&parseTime=True&loc=Local
    pool:
      min: 2
      max: 10
      lifetimeMinute: 60
      timeoutSecond: 60